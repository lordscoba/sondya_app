import 'package:flutter/widgets.dart';

class KycCodeVerificationBody extends StatelessWidget {
  const KycCodeVerificationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
