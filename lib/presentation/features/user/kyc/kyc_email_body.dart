import 'package:flutter/widgets.dart';

class KycEmailVerificationBody extends StatelessWidget {
  const KycEmailVerificationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
