import 'package:flutter/widgets.dart';

class KycCompanyInformationBody extends StatelessWidget {
  const KycCompanyInformationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
