import 'package:flutter/widgets.dart';

class ReferralPageBody extends StatelessWidget {
  const ReferralPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
