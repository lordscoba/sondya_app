import 'package:flutter/material.dart';

class OnboardingPageType {
  final String image;
  final String title;
  final String description;
  final Color? color;
  final Color? textColor;
  final Color? buttonColor;

  OnboardingPageType(this.image, this.title, this.description, this.color,
      this.textColor, this.buttonColor);
}
